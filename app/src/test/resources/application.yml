management:
  endpoints:
    web:
      base-path: /
      exposure:
        include: health,info

spring:
  jmx:
    enabled: false
  datasource:
    username: test
    password: test
  flyway:
    user: test
    password: test
  jpa:  # For debug only
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  pattern:
    level: "%5p %X{}"
